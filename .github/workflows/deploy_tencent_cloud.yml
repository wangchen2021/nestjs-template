name: NestJS Backend Auto Deploy to Tencent Cloud (Docker)
run-name: ${{ github.actor }} 部署NestJS后端 🚀 [${{ github.ref_name }}]

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      deploy_env:
        description: '部署环境'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          # - test

env:
  SERVER_PROJECT_DIR: "/usr/local/project/nestjs-template/${{ github.event.inputs.deploy_env || 'production' }}"
  NGINX_CONF_DIR: "nginx/conf.d"
  SSH_TIMEOUT: 300s

jobs:
  # 核心部署任务（原有逻辑不变）
  Deploy-to-Tencent-Cloud-Docker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ['24.11.0']
    steps:
      - name: Check out code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: '10.20.0'
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Generate build info file
        id: build_info
        run: |
          CORE_VERSION="unknown"
          FULL_VERSION="unknown-${{ github.event.inputs.deploy_env || 'prod' }}+unknown"
          COMMIT_HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
          BUILD_TIME=$(TZ=Asia/Shanghai date +"%Y-%m-%d %H:%M:%S" 2>/dev/null || echo "unknown")
          BUILD_TIME_TAG=$(TZ=Asia/Shanghai date +%Y%m%d%H%M%S 2>/dev/null || echo "unknown")

          if [ -n "${{ github.ref_name }}" ]; then
            CORE_VERSION="${{ github.ref_name }}"
          else
            TAG_VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
            CORE_VERSION=${TAG_VERSION:-$COMMIT_HASH}
          fi
          
          FULL_VERSION="${{ github.event.inputs.deploy_env || 'prod' }}-${CORE_VERSION}-${BUILD_TIME_TAG}-${COMMIT_HASH}"
          
          mkdir -p ./src/config
          cat > ./src/config/build-info.json << EOF
          {
            "coreVersion": "${CORE_VERSION}",
            "env": "${{ github.event.inputs.deploy_env || 'production' }}",
            "commitHash": "${COMMIT_HASH}",
            "buildTime": "${BUILD_TIME}",
            "fullVersion": "${FULL_VERSION}",
            "buildId": "${{ github.run_id }}-${{ github.run_attempt }}",
            "actor": "${{ github.actor }}"
          }
          EOF
          
          cat > ./src/config/build-info.env << EOF
          BUILD_CORE_VERSION=${CORE_VERSION}
          BUILD_ENV=${{ github.event.inputs.deploy_env || 'production' }}
          BUILD_COMMIT_HASH=${COMMIT_HASH}
          BUILD_TIME=${BUILD_TIME}
          BUILD_FULL_VERSION=${FULL_VERSION}
          BUILD_ID=${{ github.run_id }}-${{ github.run_attempt }}
          EOF
          
          echo "full_version=${FULL_VERSION}" >> $GITHUB_OUTPUT
          echo "core_version=${CORE_VERSION}" >> $GITHUB_OUTPUT
          echo "commit_hash=${COMMIT_HASH}" >> $GITHUB_OUTPUT
          echo "build_time=${BUILD_TIME}" >> $GITHUB_OUTPUT
          
          echo "FULL_VERSION=${FULL_VERSION}" >> $GITHUB_ENV
          echo "CORE_VERSION=${CORE_VERSION}" >> $GITHUB_ENV
          
          echo "===== 构建版本信息 ====="
          echo "Full Version: ${FULL_VERSION}"
          echo "Core Version: ${CORE_VERSION}"
          cat ./src/config/build-info.json
        env:
          NODE_ENV: ${{ github.event.inputs.deploy_env || 'production' }}
        timeout-minutes: 5

      - name: Install dependencies & Build
        run: |
          set -e
          if [ ! -f "pnpm-lock.yaml" ]; then
            echo "错误：pnpm-lock.yaml不存在！"
            exit 1
          fi
          pnpm install --frozen-lockfile --prod=false
          pnpm run build -- --verbose
          
          echo "===== 构建产物校验 ====="
          ls -la ./dist
          if [ ! -f "./dist/main.js" ]; then
            echo "构建失败：dist/main.js不存在！"
            exit 1
          fi
          if [ ! -f "./src/config/build-info.json" ]; then
            echo "错误：build-info.json未生成！"
            exit 1
          fi
          cp ./src/config/build-info* ./dist/config/
          if [ ! -f "./dist/config/build-info.json" ]; then
            echo "错误：build-info.json未复制到dist/config！"
            exit 1
          fi
        env:
          NODE_ENV: ${{ github.event.inputs.deploy_env || 'production' }}
        timeout-minutes: 10

      - name: Ensure server directory exists
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            mkdir -p ${{ env.SERVER_PROJECT_DIR }}/{${{ env.NGINX_CONF_DIR }},src/config/env,src/config,dist}
            rm -rf ${{ env.SERVER_PROJECT_DIR }}/dist/* 
            rm -rf ${{ env.SERVER_PROJECT_DIR }}/node_modules/* || true
            chown -R ${{ secrets.TENCENT_SERVER_USER }}:${{ secrets.TENCENT_SERVER_USER }} ${{ env.SERVER_PROJECT_DIR }}
            chmod -R 750 ${{ env.SERVER_PROJECT_DIR }}
            echo "服务器目录准备完成：${{ env.SERVER_PROJECT_DIR }}"

      - name: Upload config & package files
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          source: "./Dockerfile,./docker-compose.yml,./${{ env.NGINX_CONF_DIR }}/default.conf,./package.json,./pnpm-lock.yaml,./src/config/env/.env*,./src/config/build-info*"
          target: ${{ env.SERVER_PROJECT_DIR }}
          strip_components: 0
          overwrite: true
          timeout: ${{ env.SSH_TIMEOUT }}

      - name: Upload dist directory
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          source: "./dist"
          target: ${{ env.SERVER_PROJECT_DIR }}
          strip_components: 0
          overwrite: true
          timeout: ${{ env.SSH_TIMEOUT }}

      - name: Verify files on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            cd ${{ env.SERVER_PROJECT_DIR }}
            echo "===== 服务器文件校验 ====="
            REQUIRED_FILES=(
              "Dockerfile"
              "docker-compose.yml"
              "dist/main.js"
              "src/config/build-info.json"
              "dist/config/build-info.json"
            )
            for FILE in "${REQUIRED_FILES[@]}"; do
              if [ ! -f "$FILE" ]; then
                echo "错误：服务器缺少必要文件 $FILE！"
                exit 1
              fi
            done
            ls -la dist/main.js
            cat src/config/build-info.json

      - name: Build & Push Docker Image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            cd ${{ env.SERVER_PROJECT_DIR }}
            IMAGE_TAG="${{ steps.build_info.outputs.full_version }}"
            IMAGE_NAME="${{ secrets.DOCKER_PATH }}/nestjs-template:$IMAGE_TAG"
            LATEST_IMAGE="${{ secrets.DOCKER_PATH }}/nestjs-template:latest"
            
            echo "===== 构建Docker镜像 ====="
            docker image prune -f
            
            if ! docker build \
              --build-arg NODE_ENV="${{ github.event.inputs.deploy_env || 'production' }}" \
              --build-arg BUILD_VERSION="$IMAGE_TAG" \
              --build-arg BUILD_COMMIT="${{ steps.build_info.outputs.commit_hash }}" \
              --cache-from $LATEST_IMAGE \
              -t $IMAGE_NAME \
              -t $LATEST_IMAGE .; then
              echo "Docker构建失败！"
              exit 1
            fi
            
            echo "===== 推送Docker镜像 ====="
            if ! docker push $IMAGE_NAME; then
              echo "Docker推送失败！"
              exit 1
            fi
            docker push $LATEST_IMAGE
            
            echo "===== 更新docker-compose.yml ====="
            cp docker-compose.yml docker-compose.yml.bak
            sed -i.bak "s|image: chen-docker/nestjs-template.*|image: ${IMAGE_NAME}|" docker-compose.yml
            if ! grep -q "$IMAGE_NAME" docker-compose.yml; then
              echo "错误：docker-compose.yml镜像标签替换失败！"
              cp docker-compose.yml.bak docker-compose.yml
              exit 1
            fi
            rm -f docker-compose.yml.bak
            echo "替换后的镜像配置："
            cat docker-compose.yml | grep "image: "

      - name: Cleanup old Docker images
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            cd ${{ env.SERVER_PROJECT_DIR }}
            echo "===== 清理旧镜像（仅保留最新3个版本） ======"
            
            IMAGE_REPO="${{ secrets.DOCKER_PATH }}/nestjs-template"
            IMAGE_LIST=$(docker images --format "{{.Repository}}:{{.Tag}}" | grep -F "$IMAGE_REPO" | grep -v "latest" | grep -v "<none>")
            
            echo "原始扫描到的镜像列表："
            echo "$IMAGE_LIST"
            
            if [ -z "$IMAGE_LIST" ]; then
              echo "无需要清理的镜像，跳过"
              exit 0
            fi
            
            SORTED_IMAGES=$(echo "$IMAGE_LIST" | while read -r IMAGE; do
              TAG=$(echo "$IMAGE" | awk -F: '{for(i=1;i<=NF;i++){last=$i}} END{print last}')
              TIMESTAMP=$(echo "$TAG" | grep -oE '[0-9]{14}' | head -1)
              if [ -n "$TIMESTAMP" ]; then
                echo "${TIMESTAMP} ${IMAGE}"
              fi
            done | sort -nr | awk '{print $2}')
            
            echo "排序后的镜像列表（最新→最旧）："
            echo "$SORTED_IMAGES"
            
            TOTAL=$(echo "$SORTED_IMAGES" | wc -l | tr -d ' ')
            echo "有效镜像总数：$TOTAL"
            
            if [ "$TOTAL" -gt 3 ]; then
              echo "需要清理的旧镜像（总数${TOTAL}，保留前3个）："
              TO_DELETE=$(echo "$SORTED_IMAGES" | tail -n +4)
              echo "$TO_DELETE"
              
              for IMAGE in $TO_DELETE; do
                if [ -n "$IMAGE" ]; then
                  echo "====================================="
                  echo "处理镜像：$IMAGE"
                  CONTAINER_IDS=$(docker ps -a --filter "ancestor=$IMAGE" --format "{{.ID}}")
                  if [ -n "$CONTAINER_IDS" ]; then
                    echo "  - 停止并删除依赖容器：$CONTAINER_IDS"
                    docker stop $CONTAINER_IDS >/dev/null 2>&1
                    docker rm $CONTAINER_IDS >/dev/null 2>&1
                  else
                    echo "  - 无依赖容器"
                  fi
                  if docker rmi -f "$IMAGE"; then
                    echo "  ✅ 成功删除镜像：$IMAGE"
                  else
                    echo "  ❌ 删除镜像失败：$IMAGE（可能被其他资源占用）"
                  fi
                fi
              done
            else
              echo "镜像数量≤3（当前${TOTAL}个），无需清理"
            fi
            
            docker image prune -f >/dev/null 2>&1
            docker container prune -f >/dev/null 2>&1
            echo "✅ 镜像清理完成！"

      - name: Restart containers
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            cd ${{ env.SERVER_PROJECT_DIR }}
            
            echo "===== 步骤1：停止旧容器 ====="
            docker-compose stop || echo "无运行中的容器"
            
            echo "===== 步骤2：启动新容器 ====="
            docker-compose up -d --build
            
            echo "===== 步骤3：验证启动状态 ====="
            sleep 3
            if ! docker-compose ps | grep -q "Up"; then
              echo "错误：容器启动失败！"
              docker-compose logs
              exit 1
            fi
            
            echo "===== 步骤4：清理旧容器 ====="
            docker-compose rm -f || echo "无旧容器需删除"
            
            docker container prune -f >/dev/null 2>&1
            docker network prune -f >/dev/null 2>&1
            
            echo "✅ 容器启动成功！"

      - name: Verify deployment
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TENCENT_SERVER_HOST }}
          username: ${{ secrets.TENCENT_SERVER_USER }}
          key: ${{ secrets.TENCENT_SERVER_SSH_KEY }}
          port: ${{ secrets.TENCENT_SERVER_PORT }}
          timeout: ${{ env.SSH_TIMEOUT }}
          script: |
            set -e
            cd ${{ env.SERVER_PROJECT_DIR }}
            echo "===== 部署验证 ====="
            echo "--- 容器状态 ---"
            docker-compose ps
            echo "--- NGINX 日志 ---"
            docker-compose logs --tail=20 nginx
            echo "--- 后端日志 ---"
            docker-compose logs --tail=20 nestjs-backend
            
            sleep 10
            echo "--- 接口版本检查 ---"
            HEALTH_CHECK_URL="https://nestjs.duguqinchen.com/proApi/"
            echo "访问接口: $HEALTH_CHECK_URL"
            
            RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" "$HEALTH_CHECK_URL")
            HTTP_STATUS=$(echo "$RESPONSE" | grep -oP 'HTTP_STATUS:\K\d+' | tail -1)
            RESPONSE_BODY=$(echo "$RESPONSE" | sed '$ d')
            
            echo "状态码: $HTTP_STATUS"
            echo "响应内容: "
            echo "$RESPONSE_BODY" | jq . 2>/dev/null || echo "$RESPONSE_BODY"
            
            if [ "$HTTP_STATUS" != "200" ]; then
              echo "错误：接口状态码异常！"
              exit 1
            fi

  # 新增：独立的API同步任务（仅部署成功后执行）
  Sync-API-To-Apifox:
    needs: Deploy-to-Tencent-Cloud-Docker  # 依赖部署任务，仅部署成功才执行
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ['24.11.0']
    steps:
      - name: Check out code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: '10.20.0'
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies（仅安装生成API所需依赖）
        run: |
          set -e
          pnpm install --frozen-lockfile --prod=false
        timeout-minutes: 5

      - name: Generate openapi.json（生成API文档）
        run: |
          set -e
          npx ts-node scripts/generate-openapi.ts
          # 验证文件生成
          if [ ! -f "./openapi.json" ]; then
            echo "错误：openapi.json生成失败！"
            exit 1
          fi
          echo "✅ openapi.json生成成功"
        timeout-minutes: 5

      - name: Sync to Apifox（同步到Apifox）
        env:
          # 从GitHub Secrets读取Apifox凭证（需提前配置）
          APIFOX_TOKEN: ${{ secrets.APIFOX_TOKEN }}
          APIFOX_PROJECT_ID: ${{ secrets.APIFOX_PROJECT_ID }}
        run: |
          set -e
          npx ts-node scripts/sync-apifox.ts
          echo "✅ API文档同步到Apifox完成"
        timeout-minutes: 5